# Tests

The test tree is scaffolded to match the architecture proposal.

- `fixtures/cvs/` holds captured CVS metadata and command output
- `unit/` holds parser and service tests
- `integration/` holds repository harnesses and workflow tests
- `ui/` holds smoke tests for Neovim buffers and views
